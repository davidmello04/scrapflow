services:
  mongo:
    image: mongo:7
    container_name: scrapflow-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - scrapflow_mongo_data:/data/db

volumes:
  scrapflow_mongo_data:
